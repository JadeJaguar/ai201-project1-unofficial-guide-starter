sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Query interface (Milestone 5)
gradio>=6.9.0

# Document ingestion — used to extract the one PDF in documents/ (Milestone 3)
pdfplumber==0.11.9

# Stretch feature: keyword (BM25) half of hybrid search (local, no API key)
rank-bm25==0.2.2
